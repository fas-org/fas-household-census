from django.conf.urls import url
from ..views import household

urlpatterns = [
    # url(r'^new$', household.new, name='household_new'),
    # url(r'^(?P<pk>\d+)/update$', household.update, name='household_update'),
    # url(r'^get$', household.get, name='household_get'),
]